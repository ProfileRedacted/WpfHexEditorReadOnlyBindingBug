# WpfHexEditorReadOnlyBindingBug
An example of a bug in WpfHexEditor 2.1.7 demonstrating that when one-way binding is attached to the ReadOnlyMode property it gets detached after changing property value.
